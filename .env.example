# BloodInsight AI - Environment Variables Example
# Replace placeholder values with actual credentials in .env.local file
# DO NOT commit actual credentials to version control

# Gemini API key (server-side only)
GEMINI_API_KEY=your_gemini_api_key_here

# NextAuth configuration
NEXTAUTH_URL=https://your-deployment-url.vercel.app
NEXTAUTH_SECRET=your_nextauth_secret_here

# Google OAuth credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgres://username:password@hostname.region.aws.neon.tech/dbname?sslmode=require

# Maximum allowed upload size in bytes (default is 5MB)
MAX_UPLOAD_BYTES=5242880

# Sentry Configuration (for error tracking)
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here
SENTRY_USER_ID=your_sentry_user_id_here

# Optional: Additional database connection parameters
# Only needed for specific use cases
# DATABASE_URL_UNPOOLED=postgresql://username:password@hostname.region.aws.neon.tech/dbname?sslmode=require
# POSTGRES_PRISMA_URL=postgres://username:password@hostname.region.aws.neon.tech/dbname?connect_timeout=15&sslmode=require
